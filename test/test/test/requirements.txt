[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "test-recursive-python-workspace"
version = "1.0.0"
description = "Testing recursive workspace scanning for Python"
authors = [
    {name = "Test User", email = "test@example.com"},
]
requires-python = ">=3.8"
dependencies = [
    "requests123213123123123213>=2.28.0",
    "pydantic12321321312321312321312312>=1.10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=22.0.0",
    "-e ./packages/data-processing",
    "-e ./shared-libs/ml-utils",
]
workspace-deps = [
    "-e ./packages/*",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
